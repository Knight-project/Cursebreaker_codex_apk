import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nyxeros.cursebreakercodex',
  appName: 'Cursebreaker_codex',
  webDir: 'out'
};

export default config;
